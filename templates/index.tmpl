<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Задания</title>
    <link rel="stylesheet" href="/static/styles.css">
</head>
<body>
    <header class="header">
        <div class="logo">
            <img src="{{.logo1}}" alt="Логотип">
        </div>
        <nav class="nav">
            <a href="#">Задания</a>
            <a href="#">Заявки</a>
        </nav>
    </header>
    <main>
        <h1>Задания</h1>
        <div class="search-bar">
            <input type="text" placeholder="Поиск заданий">
        </div>


<div class="task-list">
    {{range .tasks}}
    <div class="task-item">
        <img src="{{.Image}}" alt="Задание {{.ID}}" width="150" height="150">
        <div class="task-info">
            <h2>{{.Title}}</h2>
            <p>{{.Description}}</p>
            <span class="hours">{{.Hours}} часов на выполнение</span>
            <a href="/task/{{ .ID }}" class="button12">Подробнее</a>
        </div>

    </div>
    {{end}}
</div>
</main>
</body>
</html>